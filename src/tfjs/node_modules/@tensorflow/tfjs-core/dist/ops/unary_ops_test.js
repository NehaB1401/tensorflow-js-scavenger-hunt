"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var dl = require("../index");
var test_util_1 = require("../test_util");
var util = require("../util");
var selu_util = require("./selu_util");
test_util_1.describeWithFlags('relu', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([1, -2, 0, 3, -0.1]);
        var result = dl.relu(a);
        test_util_1.expectArraysClose(result, [1, 0, 0, 3, 0]);
    });
    it('does nothing to positive values', function () {
        var a = dl.scalar(1);
        var result = dl.relu(a);
        test_util_1.expectNumbersClose(result.get(), 1);
    });
    it('sets negative values to 0', function () {
        var a = dl.scalar(-1);
        var result = dl.relu(a);
        test_util_1.expectNumbersClose(result.get(), 0);
    });
    it('preserves zero values', function () {
        var a = dl.scalar(0);
        var result = dl.relu(a);
        test_util_1.expectNumbersClose(result.get(), 0);
    });
    it('propagates NaNs, float32', function () {
        var a = dl.tensor1d([1, -2, 0, 3, -0.1, NaN]);
        var result = dl.relu(a);
        expect(result.dtype).toBe('float32');
        test_util_1.expectArraysClose(result, [1, 0, 0, 3, 0, NaN]);
    });
    it('propagates NaNs, int32', function () {
        var a = dl.tensor1d([1, -2, 0, 3, -1, util.NAN_INT32], 'int32');
        var result = dl.relu(a);
        expect(result.dtype).toBe('int32');
        test_util_1.expectArraysClose(result, [1, 0, 0, 3, 0, util.NAN_INT32]);
    });
    it('propagates NaNs, bool', function () {
        var a = dl.tensor1d([1, 0, 0, 1, 0, util.NAN_BOOL], 'bool');
        var result = dl.relu(a);
        expect(result.dtype).toBe('bool');
        test_util_1.expectArraysClose(result, [1, 0, 0, 1, 0, util.NAN_BOOL]);
    });
    it('gradients: positive scalar', function () {
        var a = dl.scalar(3);
        var dy = dl.scalar(5);
        var grad = dl.grad(function (a) { return dl.relu(a); });
        var da = grad(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [5]);
    });
    it('gradients: negative scalar', function () {
        var a = dl.scalar(-3);
        var dy = dl.scalar(5);
        var grad = dl.grad(function (a) { return dl.relu(a); });
        var da = grad(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [0]);
    });
    it('gradients: array', function () {
        var a = dl.tensor2d([1, -1, 0, .1], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var grad = dl.grad(function (a) { return dl.relu(a); });
        var da = grad(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [1, 0, 0, 4]);
    });
});
test_util_1.describeWithFlags('abs', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([1, -2, 0, 3, -0.1]);
        var result = dl.abs(a);
        test_util_1.expectArraysClose(result, [1, 2, 0, 3, 0.1]);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([1, -2, 0, 3, -0.1, NaN]);
        var result = dl.abs(a);
        test_util_1.expectArraysClose(result, [1, 2, 0, 3, 0.1, NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(4);
        var dy = dl.scalar(8);
        var da = dl.grad(function (a) { return dl.abs(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [8 * 1]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([1, 2, -3, 5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var da = dl.grad(function (a) { return dl.abs(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [1 * 1, 2 * 1, 3 * -1, 4 * 1]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([3, -1, -2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var da = dl.grad(function (a) { return dl.abs(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [1 * 1, 2 * -1, 3 * -1, 4 * 1]);
    });
});
test_util_1.describeWithFlags('step', test_util_1.ALL_ENVS, function () {
    it('with 1d tensor', function () {
        var a = dl.tensor1d([1, -2, -.01, 3, -0.1]);
        var result = dl.step(a);
        test_util_1.expectArraysClose(result, [1, 0, 0, 1, 0]);
    });
    it('with 1d tensor and alpha', function () {
        var a = dl.tensor1d([1, -2, -.01, 3, NaN]);
        var result = dl.step(a, 0.1);
        test_util_1.expectArraysClose(result, [1, 0.1, 0.1, 1, NaN]);
    });
    it('with 2d tensor', function () {
        var a = dl.tensor2d([1, -5, -3, 4], [2, 2]);
        var result = dl.step(a);
        expect(result.shape).toEqual([2, 2]);
        test_util_1.expectArraysClose(result, [1, 0, 0, 1]);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([1, -2, -.01, 3, NaN]);
        var result = dl.step(a);
        test_util_1.expectArraysClose(result, [1, 0, 0, 1, NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(-4);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.step(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([1, 2, -3, 5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.step(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([3, -1, -2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.step(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
});
test_util_1.describeWithFlags('neg', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([1, -3, 2, 7, -4]);
        var result = dl.neg(a);
        test_util_1.expectArraysClose(result, [-1, 3, -2, -7, 4]);
    });
    it('propagate NaNs', function () {
        var a = dl.tensor1d([1, -3, 2, 7, NaN]);
        var result = dl.neg(a);
        var expected = [-1, 3, -2, -7, NaN];
        test_util_1.expectArraysClose(result, expected);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(4);
        var dy = dl.scalar(8);
        var da = dl.grad(function (a) { return dl.neg(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [8 * -1]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([1, 2, -3, 5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var da = dl.grad(function (a) { return dl.neg(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [1 * -1, 2 * -1, 3 * -1, 4 * -1]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([3, -1, -2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var da = dl.grad(function (a) { return dl.neg(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [1 * -1, 2 * -1, 3 * -1, 4 * -1]);
    });
});
test_util_1.describeWithFlags('sigmoid', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.sigmoid(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = 1 / (1 + Math.exp(-values[i]));
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([3, NaN]);
        var res = dl.sigmoid(a);
        test_util_1.expectArraysClose(res, [1 / (1 + Math.exp(-3)), NaN]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([1, 2, -3, 5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var da = dl.grad(function (a) { return dl.sigmoid(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            var y = 1 / (1 + Math.exp(-a.get(i)));
            expected[i] = dy.get(i) * y * (1 - y);
        }
        test_util_1.expectArraysClose(da, expected);
    });
});
test_util_1.describeWithFlags('sqrt', test_util_1.ALL_ENVS, function () {
    it('sqrt', function () {
        var a = dl.tensor1d([2, 4]);
        var r = dl.sqrt(a);
        test_util_1.expectNumbersClose(r.get(0), Math.sqrt(2));
        test_util_1.expectNumbersClose(r.get(1), Math.sqrt(4));
    });
    it('sqrt propagates NaNs', function () {
        var a = dl.tensor1d([1, NaN]);
        var r = dl.sqrt(a);
        test_util_1.expectArraysClose(r, [Math.sqrt(1), NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(4);
        var dy = dl.scalar(8);
        var da = dl.grad(function (a) { return dl.sqrt(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [8 / (2 * Math.sqrt(4))]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([1, 2, 3, 5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.sqrt(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            1 / (2 * Math.sqrt(1)), 2 / (2 * Math.sqrt(2)),
            3 / (2 * Math.sqrt(3)), 4 / (2 * Math.sqrt(5))
        ], 1e-1);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.sqrt(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            1 / (2 * Math.sqrt(3)), 2 / (2 * Math.sqrt(1)),
            3 / (2 * Math.sqrt(2)), 4 / (2 * Math.sqrt(3))
        ], 1e-1);
    });
});
test_util_1.describeWithFlags('rsqrt', test_util_1.ALL_ENVS, function () {
    it('rsqrt', function () {
        var a = dl.tensor1d([2, 4]);
        var r = dl.rsqrt(a);
        test_util_1.expectNumbersClose(r.get(0), 1 / Math.sqrt(2));
        test_util_1.expectNumbersClose(r.get(1), 1 / Math.sqrt(4));
    });
    it('rsqrt propagates NaNs', function () {
        var a = dl.tensor1d([1, NaN]);
        var r = dl.rsqrt(a);
        test_util_1.expectArraysClose(r, [1 / Math.sqrt(1), NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(4);
        var dy = dl.scalar(8);
        var da = dl.grad(function (a) { return dl.rsqrt(a); })(a, dy);
        expect(da.shape).toEqual(a.shape);
        expect(da.dtype).toEqual('float32');
        test_util_1.expectArraysClose(da, [(-1 * 8) / (2 * Math.pow(4, 1.5))]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([1, 2, 3, 5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.rsqrt(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            -1 * 1 / (2 * Math.pow(1, 1.5)), -1 * 2 / (2 * Math.pow(2, 1.5)),
            -1 * 3 / (2 * Math.pow(3, 1.5)), -1 * 4 / (2 * Math.pow(5, 1.5))
        ], 1e-1);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.rsqrt(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            -1 * 1 / (2 * Math.pow(3, 1.5)), -1 * 2 / (2 * Math.pow(1, 1.5)),
            -1 * 3 / (2 * Math.pow(2, 1.5)), -1 * 4 / (2 * Math.pow(3, 1.5))
        ], 1e-1);
    });
});
test_util_1.describeWithFlags('square', test_util_1.ALL_ENVS, function () {
    it('1D array', function () {
        var a = dl.tensor1d([2, 4, Math.sqrt(2)]);
        var r = dl.square(a);
        test_util_1.expectArraysClose(r, [4, 16, 2]);
    });
    it('2D array', function () {
        var a = dl.tensor2d([1, 2, Math.sqrt(2), Math.sqrt(3)], [2, 2]);
        var r = dl.square(a);
        expect(r.shape).toEqual([2, 2]);
        test_util_1.expectArraysClose(r, [1, 4, 2, 3]);
    });
    it('square propagates NaNs', function () {
        var a = dl.tensor1d([1.5, NaN]);
        var r = dl.square(a);
        test_util_1.expectArraysClose(r, [2.25, NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.square(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [2 * 5 * 8]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.square(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [-2, 4 * 2, 6 * 3, -10 * 4]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.square(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [-6 * 1, 2 * 2, 4 * 3, 6 * 4]);
    });
});
test_util_1.describeWithFlags('reciprocal', test_util_1.ALL_ENVS, function () {
    it('1D array', function () {
        var a = dl.tensor1d([2, 3, 0, NaN]);
        var r = dl.reciprocal(a);
        test_util_1.expectArraysClose(r, [1 / 2, 1 / 3, Infinity, NaN]);
    });
    it('2D array', function () {
        var a = dl.tensor2d([1, Infinity, 0, NaN], [2, 2]);
        var r = dl.reciprocal(a);
        expect(r.shape).toEqual([2, 2]);
        test_util_1.expectArraysClose(r, [1 / 1, 0, Infinity, NaN]);
    });
    it('reciprocal propagates NaNs', function () {
        var a = dl.tensor1d([1.5, NaN]);
        var r = dl.reciprocal(a);
        test_util_1.expectArraysClose(r, [1 / 1.5, NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.reciprocal(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [-1 * 8 * (1 / (5 * 5))]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.reciprocal(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            -1 * 1 * (1 / (-1 * -1)), -1 * 2 * (1 / (2 * 2)), -1 * 3 * (1 / (3 * 3)),
            -1 * 4 * (1 / (-5 * -5))
        ]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-1, 2, 3, -5], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.reciprocal(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            -1 * 1 * (1 / (-1 * -1)), -1 * 2 * (1 / (2 * 2)), -1 * 3 * (1 / (3 * 3)),
            -1 * 4 * (1 / (-5 * -5))
        ]);
    });
});
test_util_1.describeWithFlags('log', test_util_1.ALL_ENVS, function () {
    it('log', function () {
        var a = dl.tensor1d([1, 2]);
        var r = dl.log(a);
        test_util_1.expectNumbersClose(r.get(0), Math.log(1));
        test_util_1.expectNumbersClose(r.get(1), Math.log(2));
    });
    it('log propagates NaNs', function () {
        var a = dl.tensor1d([1, NaN]);
        var r = dl.log(a);
        test_util_1.expectArraysClose(r, [Math.log(1), NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.log(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [3 / 5]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.log(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 / -1, 2 / 2, 3 / 3, 4 / -5]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.log(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 / -3, 2 / 1, 3 / 2, 4 / 3]);
    });
});
test_util_1.describeWithFlags('log1p', test_util_1.ALL_ENVS, function () {
    it('log1p', function () {
        var a = dl.tensor1d([1, 2]);
        var r = dl.log1p(a);
        test_util_1.expectNumbersClose(r.get(0), Math.log1p(1));
        test_util_1.expectNumbersClose(r.get(1), Math.log1p(2));
    });
    it('log1p propagates NaNs', function () {
        var a = dl.tensor1d([1, NaN]);
        var r = dl.log1p(a);
        test_util_1.expectArraysClose(r, [Math.log1p(1), NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.log1p(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [3 / (1 + 5)]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.log1p(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [Infinity, 2 / (1 + 2), 3 / (1 + 3), 4 / (1 + -5)]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.log1p(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 / (1 + -3), 2 / (1 + 1), 3 / (1 + 2), 4 / (1 + 3)]);
    });
});
test_util_1.describeWithFlags('ceil', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([1.5, 2.1, -1.4]);
        var r = dl.ceil(a);
        test_util_1.expectNumbersClose(r.get(0), 2);
        test_util_1.expectNumbersClose(r.get(1), 3);
        test_util_1.expectNumbersClose(r.get(2), -1);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([1.5, NaN, -1.4]);
        var r = dl.ceil(a);
        test_util_1.expectArraysClose(r, [2, NaN, -1]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5.2);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.ceil(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1.1, 2.6, 3, -5.9]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.ceil(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2.2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.ceil(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
});
test_util_1.describeWithFlags('floor', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([1.5, 2.1, -1.4]);
        var r = dl.floor(a);
        test_util_1.expectNumbersClose(r.get(0), 1);
        test_util_1.expectNumbersClose(r.get(1), 2);
        test_util_1.expectNumbersClose(r.get(2), -2);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([1.5, NaN, -1.4]);
        var r = dl.floor(a);
        test_util_1.expectArraysClose(r, [1, NaN, -2]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5.2);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.floor(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1.1, 2.6, 3, -5.9]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.floor(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2.2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.floor(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
});
test_util_1.describeWithFlags('sign', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([1.5, 0, NaN, -1.4]);
        var r = dl.sign(a);
        test_util_1.expectNumbersClose(r.get(0), 1);
        test_util_1.expectNumbersClose(r.get(1), 0);
        test_util_1.expectNumbersClose(r.get(2), 0);
        test_util_1.expectNumbersClose(r.get(3), -1);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([1.5, NaN, -1.4]);
        var r = dl.sign(a);
        test_util_1.expectArraysClose(r, [1, 0, -1]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5.2);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.sign(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1.1, 2.6, 3, -5.9]);
        var dy = dl.tensor1d([-1, 1, 1, -1]);
        var gradients = dl.grad(function (a) { return dl.sign(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2.2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.sign(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
});
test_util_1.describeWithFlags('exp', test_util_1.ALL_ENVS, function () {
    it('exp', function () {
        var a = dl.tensor1d([1, 2, 0]);
        var r = dl.exp(a);
        test_util_1.expectNumbersClose(r.get(0), Math.exp(1));
        test_util_1.expectNumbersClose(r.get(1), Math.exp(2));
        test_util_1.expectNumbersClose(r.get(2), 1);
    });
    it('exp propagates NaNs', function () {
        var a = dl.tensor1d([1, NaN, 0]);
        var r = dl.exp(a);
        test_util_1.expectArraysClose(r, [Math.exp(1), NaN, 1]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.exp(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [3 * Math.exp(0.5)]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.exp(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 * Math.exp(-1), 2 * Math.exp(2), 3 * Math.exp(3), 4 * Math.exp(-5)], 1e-1);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.exp(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 * Math.exp(-3), 2 * Math.exp(1), 3 * Math.exp(2), 4 * Math.exp(3)], 1e-1);
    });
});
test_util_1.describeWithFlags('expm1', test_util_1.ALL_ENVS, function () {
    it('expm1', function () {
        var a = dl.tensor1d([1, 2, 0]);
        var r = dl.expm1(a);
        test_util_1.expectNumbersClose(r.get(0), Math.expm1(1));
        test_util_1.expectNumbersClose(r.get(1), Math.expm1(2));
        test_util_1.expectNumbersClose(r.get(2), Math.expm1(0));
    });
    it('expm1 propagates NaNs', function () {
        var a = dl.tensor1d([1, NaN, 0]);
        var r = dl.expm1(a);
        test_util_1.expectArraysClose(r, [Math.expm1(1), NaN, Math.expm1(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.expm1(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [3 * Math.exp(0.5)]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.expm1(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 * Math.exp(-1), 2 * Math.exp(2), 3 * Math.exp(3), 4 * Math.exp(-5)], 1e-1);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.expm1(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 * Math.exp(-3), 2 * Math.exp(1), 3 * Math.exp(2), 4 * Math.exp(3)], 1e-1);
    });
});
test_util_1.describeWithFlags('sin', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.sin(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.sin(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.sin(a);
        test_util_1.expectArraysClose(res, [Math.sin(4), NaN, Math.sin(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.sin(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 * Math.cos(5)]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.sin(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 * Math.cos(-1), 2 * Math.cos(2), 3 * Math.cos(3), 4 * Math.cos(-5)], 1e-1);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.sin(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1 * Math.cos(-3), 2 * Math.cos(1), 3 * Math.cos(2), 4 * Math.cos(3)], 1e-1);
    });
});
test_util_1.describeWithFlags('cos', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.cos(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.cos(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.cos(a);
        test_util_1.expectArraysClose(res, [Math.cos(4), NaN, Math.cos(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.cos(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 * Math.sin(5) * -1]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1, 2, 3, -5]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.cos(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            1 * Math.sin(-1) * -1, 2 * Math.sin(2) * -1, 3 * Math.sin(3) * -1,
            4 * Math.sin(-5) * -1
        ], 1e-1);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.cos(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [
            1 * Math.sin(-3) * -1, 2 * Math.sin(1) * -1, 3 * Math.sin(2) * -1,
            4 * Math.sin(3) * -1
        ], 1e-1);
    });
});
test_util_1.describeWithFlags('tan', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.tan(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.tan(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.tan(a);
        test_util_1.expectArraysClose(res, [Math.tan(4), NaN, Math.tan(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.tan(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 / (Math.cos(0.5) * Math.cos(0.5))]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-1, 2, 3, -5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.tan(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / (Math.cos(aValues[i]) * Math.cos(aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-3, 1, 2, 3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.tan(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / (Math.cos(aValues[i]) * Math.cos(aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('asin', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [.1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.asin(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.asin(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.asin(a);
        test_util_1.expectArraysClose(res, [Math.asin(4), NaN, Math.asin(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.asin(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 / Math.sqrt(1 - (0.5 * 0.5))]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-0.1, 0.2, 0.3, -0.5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.asin(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / Math.sqrt(1 - (aValues[i] * aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-0.3, 0.1, 0.2, 0.3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.asin(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / Math.sqrt(1 - (aValues[i] * aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('acos', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [.1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.acos(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.acos(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.acos(a);
        test_util_1.expectArraysClose(res, [Math.acos(4), NaN, Math.acos(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.acos(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [(-1 * 8) / Math.sqrt(1 - (0.5 * 0.5))]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-0.1, 0.2, 0.3, -0.5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.acos(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] =
                (-1 * dyValues[i]) / Math.sqrt(1 - (aValues[i] * aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-0.3, 0.1, 0.2, 0.3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.acos(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] =
                (-1 * dyValues[i]) / Math.sqrt(1 - (aValues[i] * aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('atan', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.atan(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.atan(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.atan(a);
        test_util_1.expectArraysClose(res, [Math.atan(4), NaN, Math.atan(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.atan(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 / (1 + (0.5 * 0.5))]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-0.1, 0.2, 0.3, -0.5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.atan(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / (1 + (aValues[i] * aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-0.3, 0.1, 0.2, 0.3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.atan(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / (1 + (aValues[i] * aValues[i]));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('sinh', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.sinh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.sinh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.sinh(a);
        test_util_1.expectArraysClose(res, [Math.sinh(4), NaN, Math.sinh(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.sinh(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 * Math.cosh(0.5)]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-1, 2, 3, -5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.sinh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] * Math.cosh(aValues[i]);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-3, 1, 2, 3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.sinh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] * Math.cosh(aValues[i]);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('cosh', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, -1, -4];
        var a = dl.tensor1d(values);
        var result = dl.cosh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.cosh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.cosh(a);
        test_util_1.expectArraysClose(res, [Math.cosh(4), NaN, Math.cosh(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.cosh(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 * Math.sinh(0.5)]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-1, 2, 3, -5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.cosh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] * Math.sinh(aValues[i]);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-3, 1, 2, 3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.cosh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] * Math.sinh(aValues[i]);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('tanh', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.tanh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = util.tanh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.tanh(a);
        test_util_1.expectArraysClose(res, [util.tanh(4), NaN, util.tanh(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.tanh(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 * (1 - (Math.tanh(0.5) * Math.tanh(0.5)))]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-1, 2, 3, -5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.tanh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] =
                dyValues[i] * (1 - (Math.tanh(aValues[i]) * Math.tanh(aValues[i])));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-3, 1, 2, 3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.tanh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] =
                dyValues[i] * (1 - (Math.tanh(aValues[i]) * Math.tanh(aValues[i])));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('leakyRelu', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([0, 1, -2]);
        var result = dl.leakyRelu(a);
        expect(result.shape).toEqual(a.shape);
        test_util_1.expectArraysClose(result, [0, 1, -0.4]);
    });
    it('propagates NaN', function () {
        var a = dl.tensor1d([0, 1, NaN]);
        var result = dl.leakyRelu(a);
        expect(result.shape).toEqual(a.shape);
        test_util_1.expectArraysClose(result, [0, 1, NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(-4);
        var dy = dl.scalar(8);
        var alpha = 0.1;
        var gradients = dl.grad(function (a) { return dl.leakyRelu(a, alpha); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 * alpha]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [1, -1, 0.1];
        var dyValues = [1, 2, 3];
        var alpha = 0.1;
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.leakyRelu(a, alpha); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1, 2 * alpha, 3]);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [1, -1, 0.1, 0.5];
        var dyValues = [1, 2, 3, 4];
        var alpha = 0.1;
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.leakyRelu(a, alpha); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [1, 2 * alpha, 3, 4]);
    });
});
test_util_1.describeWithFlags('elu', test_util_1.ALL_ENVS, function () {
    it('calculate elu', function () {
        var a = dl.tensor1d([1, -1, 0]);
        var result = dl.elu(a);
        expect(result.shape).toEqual(a.shape);
        test_util_1.expectArraysClose(result, [1, -0.6321, 0]);
    });
    it('elu propagates NaN', function () {
        var a = dl.tensor1d([1, NaN]);
        var result = dl.elu(a);
        expect(result.shape).toEqual(a.shape);
        test_util_1.expectArraysClose(result, [1, NaN]);
    });
    it('derivative', function () {
        var x = dl.tensor1d([1, 3, -2]);
        var dy = dl.tensor1d([5, 50, 500]);
        var gradients = dl.grad(function (a) { return dl.elu(a); })(x, dy);
        expect(gradients.shape).toEqual(x.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [5, 50, 500 * Math.exp(-2)]);
    });
});
test_util_1.describeWithFlags('selu', test_util_1.ALL_ENVS, function () {
    var scaleAlpha = selu_util.SELU_SCALEALPHA;
    var scale = selu_util.SELU_SCALE;
    it('calculate selu', function () {
        var a = dl.tensor1d([1, -1, 0]);
        var result = dl.selu(a);
        expect(result.shape).toEqual(a.shape);
        test_util_1.expectArraysClose(result, [1.0507, -1.1113, 0]);
    });
    it('selu propagates NaN', function () {
        var a = dl.tensor1d([1, NaN]);
        var result = dl.selu(a);
        expect(result.shape).toEqual(a.shape);
        test_util_1.expectArraysClose(result, [1.0507, NaN]);
    });
    it('gradients: Scalar', function () {
        var aValue = 1;
        var dyValue = 1;
        var a = dl.scalar(aValue);
        var dy = dl.scalar(dyValue);
        var gradients = dl.grad(function (a) { return dl.selu(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [dyValue * scale]);
        aValue = -1;
        dyValue = 2;
        a = dl.scalar(aValue);
        dy = dl.scalar(dyValue);
        gradients = dl.grad(function (a) { return dl.selu(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [dyValue * scaleAlpha * Math.exp(aValue)]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [1, -1, 0];
        var dyValues = [1, 2, 3];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.selu(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            if (aValues[i] > 0) {
                expected[i] = dyValues[i] * scale;
            }
            else {
                expected[i] = dyValues[i] * scaleAlpha * Math.exp(aValues[i]);
            }
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [1, -1, 0, 0.5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.selu(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            if (aValues[i] > 0) {
                expected[i] = dyValues[i] * scale;
            }
            else {
                expected[i] = dyValues[i] * scaleAlpha * Math.exp(aValues[i]);
            }
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('clip', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([3, -1, 0, 100, -7, 2]);
        var min = -1;
        var max = 50;
        var result = dl.clipByValue(a, min, max);
        test_util_1.expectArraysClose(result, [3, -1, 0, 50, -1, 2]);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([3, -1, 0, 100, -7, 2, NaN]);
        var min = -1;
        var max = 50;
        var result = dl.clipByValue(a, min, max);
        test_util_1.expectArraysClose(result, [3, -1, 0, 50, -1, 2, NaN]);
    });
    it('min greater than max', function () {
        var a = dl.tensor1d([3, -1, 0, 100, -7, 2]);
        var min = 1;
        var max = -1;
        var f = function () {
            dl.clipByValue(a, min, max);
        };
        expect(f).toThrowError();
    });
    it('derivative: 1D tensor', function () {
        var min = -1;
        var max = 2;
        var x = dl.tensor1d([3, -2, 1]);
        var dy = dl.tensor1d([5, 50, 500]);
        var gradients = dl.grad(function (x) { return x.clipByValue(min, max); })(x, dy);
        expect(gradients.shape).toEqual(x.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 500]);
    });
    it('derivative: scalar', function () {
        var min = -1;
        var max = 2;
        var x = dl.scalar(-10);
        var dy = dl.scalar(5);
        var gradients = dl.grad(function (x) { return x.clipByValue(min, max); })(x, dy);
        expect(gradients.shape).toEqual(x.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0]);
    });
});
test_util_1.describeWithFlags('round', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var a = dl.tensor1d([0.9, 2.5, 2.3, 1.5, -4.5]);
        var r = dl.round(a);
        test_util_1.expectNumbersClose(r.get(0), 1.0);
        test_util_1.expectNumbersClose(r.get(1), 2.0);
        test_util_1.expectNumbersClose(r.get(2), 2.0);
        test_util_1.expectNumbersClose(r.get(3), 2.0);
        test_util_1.expectNumbersClose(r.get(4), -4.0);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([1.5, NaN, -1.4]);
        var r = dl.round(a);
        test_util_1.expectArraysClose(r, [2, NaN, -1]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(5.2);
        var dy = dl.scalar(3);
        var gradients = dl.grad(function (a) { return dl.round(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0]);
    });
    it('gradients: Tensor1D', function () {
        var a = dl.tensor1d([-1.1, 2.6, 3, -5.9]);
        var dy = dl.tensor1d([1, 2, 3, 4]);
        var gradients = dl.grad(function (a) { return dl.round(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
    it('gradients: Tensor2D', function () {
        var a = dl.tensor2d([-3, 1, 2.2, 3], [2, 2]);
        var dy = dl.tensor2d([1, 2, 3, 4], [2, 2]);
        var gradients = dl.grad(function (a) { return dl.round(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [0, 0, 0, 0]);
    });
});
test_util_1.describeWithFlags('asinh', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [1, -3, 2, 7, -4];
        var a = dl.tensor1d(values);
        var result = dl.asinh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.asinh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('scalar', function () {
        var a = dl.scalar(1);
        var result = dl.asinh(a);
        var expected = [Math.asinh(1)];
        test_util_1.expectArraysClose(result, expected);
    });
    it('tensor2D', function () {
        var values = [1, -3, 2, 7];
        var a = dl.tensor2d(values, [2, 2]);
        var result = dl.asinh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.asinh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 0]);
        var res = dl.asinh(a);
        test_util_1.expectArraysClose(res, [Math.asinh(4), NaN, Math.asinh(0)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.asinh(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 / Math.sqrt(1.0 + 0.5 * 0.5)]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-1, 2, 3, -5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.asinh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / Math.sqrt(1 + aValues[i] * aValues[i]);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-3, 1, 2, 3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.asinh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / Math.sqrt(1 + aValues[i] * aValues[i]);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('acosh', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [2, 3, 4, 5, 6];
        var a = dl.tensor1d(values);
        var result = dl.acosh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.acosh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('scalar', function () {
        var value = 2;
        var a = dl.scalar(value);
        var result = dl.acosh(a);
        var expected = [Math.acosh(value)];
        test_util_1.expectArraysClose(result, expected);
    });
    it('tensor2d', function () {
        var values = [2, 3, 4, 5];
        var a = dl.tensor2d(values, [2, 2]);
        var result = dl.acosh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.acosh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([4, NaN, 2]);
        var res = dl.acosh(a);
        test_util_1.expectArraysClose(res, [Math.acosh(4), NaN, Math.acosh(2)]);
    });
    it('NaN outside function domain', function () {
        var a = dl.tensor1d([4, -1, 2]);
        var res = dl.acosh(a);
        test_util_1.expectArraysClose(res, [Math.acosh(4), NaN, Math.acosh(2)]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(1.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.acosh(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8.0 / Math.sqrt(1.5 * 1.5 - 1.0)]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [2, 3, 5, 10];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.acosh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / Math.sqrt(Math.pow(aValues[i], 2) - 1.0);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [2, 3, 5, 7];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.acosh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / Math.sqrt(Math.pow(aValues[i], 2) - 1.0);
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
test_util_1.describeWithFlags('atanh', test_util_1.ALL_ENVS, function () {
    it('basic', function () {
        var values = [-0.25, 0.25, 0.5, .75, -0.4];
        var a = dl.tensor1d(values);
        var result = dl.atanh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.atanh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('scalar', function () {
        var value = 0.2;
        var a = dl.scalar(value);
        var result = dl.atanh(a);
        var expected = [Math.atanh(value)];
        test_util_1.expectArraysClose(result, expected);
    });
    it('tensor2d', function () {
        var values = [0.2, 0.3, 0.4, 0.5];
        var a = dl.tensor2d(values, [2, 2]);
        var result = dl.atanh(a);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = Math.atanh(values[i]);
        }
        test_util_1.expectArraysClose(result, expected);
    });
    it('propagates NaNs', function () {
        var a = dl.tensor1d([0.5, NaN, 0]);
        var res = dl.atanh(a);
        test_util_1.expectArraysClose(res, [Math.atanh(0.5), NaN, Math.atanh(0)]);
    });
    it('NaN outside function domain', function () {
        var a = dl.tensor1d([-2, 0, 2]);
        var res = dl.atanh(a);
        test_util_1.expectArraysClose(res, [NaN, Math.atanh(0), NaN]);
    });
    it('gradients: Scalar', function () {
        var a = dl.scalar(0.5);
        var dy = dl.scalar(8);
        var gradients = dl.grad(function (a) { return dl.atanh(a); })(a, dy);
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, [8 / (1 - 0.5 * 0.5)]);
    });
    it('gradients: Tensor1D', function () {
        var aValues = [-0.1, 0.2, 0.3, -0.5];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor1d(aValues);
        var dy = dl.tensor1d(dyValues);
        var gradients = dl.grad(function (a) { return dl.atanh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / (1 - Math.pow(aValues[i], 2));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
    it('gradients: Tensor2D', function () {
        var aValues = [-0.3, 0.1, 0.2, 0.3];
        var dyValues = [1, 2, 3, 4];
        var a = dl.tensor2d(aValues, [2, 2]);
        var dy = dl.tensor2d(dyValues, [2, 2]);
        var gradients = dl.grad(function (a) { return dl.atanh(a); })(a, dy);
        var expected = [];
        for (var i = 0; i < a.size; i++) {
            expected[i] = dyValues[i] / (1 - Math.pow(aValues[i], 2));
        }
        expect(gradients.shape).toEqual(a.shape);
        expect(gradients.dtype).toEqual('float32');
        test_util_1.expectArraysClose(gradients, expected);
    });
});
